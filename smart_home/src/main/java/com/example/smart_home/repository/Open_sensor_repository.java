package com.example.smart_home.repository;

import com.example.smart_home.entity.Open_sensor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Open_sensor_repository extends JpaRepository<Open_sensor, Integer> {
}
